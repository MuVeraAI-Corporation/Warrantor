{
  "name": "warrantor-typescript",
  "private": true,
  "version": "1.0.0",
  "description": "AumOS TypeScript workspace \u2014 console, MCP gateway, arena, AumOS MCP server",
  "license": "Apache-2.0",
  "workspaces": [
    "console",
    "mcp-gateway",
    "arena",
    "mcp-server",
    "protocol-contracts"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck:conformance": "tsc -p tsconfig.conformance.json",
    "test": "vitest run",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "clean": "rm -rf */dist */node_modules"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^20.0.0",
    "eslint": "^10.8.1",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.66.0",
    "vitest": "^4.1.10"
  }
}
