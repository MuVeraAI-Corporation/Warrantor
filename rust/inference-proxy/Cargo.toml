[package]
name = "warrantor-inference-proxy"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "N3 — LLM inference gateway (auth, rate-limit, prompt-filter, semantic cache)"

[lib]
name = "warrantor_inference_proxy"
path = "src/lib.rs"

[dependencies]
thiserror.workspace = true
serde.workspace = true
serde_json = "1"
sha2 = "0.11"
hex = "0.4"

[dev-dependencies]
proptest = "1"
