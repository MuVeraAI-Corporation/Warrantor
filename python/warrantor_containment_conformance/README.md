# warrantor-containment-conformance (Python)

**W3 Containment conformance — Python verify path for H.R. 9917 kill-switch conformance reports.**

The Rust suite is the authoritative implementation; this package verifies the signed reports.

## Test

```bash
PYTHONPATH=src pytest tests/ -v
```
